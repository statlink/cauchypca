#useDynLib(pchc, .registration = TRUE)

importFrom("Rfast", cauchy.mle, eachcol.apply, mad2,
           spat.med, colMads, eachrow)

importFrom("stats", rnorm)

import("doParallel")
import("parallel")
import("foreach")

exportPattern("^[[:alpha:]]+")
exportPattern("^[^\\.]")


export(cauchy.pca)
